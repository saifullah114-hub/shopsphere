import Image from "../ImageShimmer";

const Loader = () => {
  return (
    <Image
      src={"/images/iwc-loader.gif"}
      alt='iwc-loader'
      imageWidth={70}
      imageHeight={70}
    />
  );
};

export default Loader;
